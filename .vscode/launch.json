{
  "configurations": [
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "Chrome localhost",
      "url": "http://127.0.0.1:4000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
